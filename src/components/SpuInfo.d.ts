import React from "react";
import { ComValue } from './index';

interface SpuInfoProps {
  topList: unknown,
  bottomList: unknown,
  mainPic: unknown,
  style: unknown
}


declare const SpuInfo: React.FC<SpuInfoProps>;

export default SpuInfo;